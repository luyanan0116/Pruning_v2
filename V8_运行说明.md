# V8 运行说明：论文方法驱动的 50% 权重置零

## 1. 四级消融

默认脚本依次运行：

1. `paper_mi`：频域互信息贡献谱；
2. `paper_mi_gb`：互信息 + 粒球多粒度局部估计；
3. `paper_mi_gb_lcb`：上一阶段 + 样本/场景双源重复估计 + LCB；
4. `paper_full`：LCB + 频带欠覆盖补偿 + 全局参数预算。

四个实验都重新加载一份原始模型；共享任务梯度响应缓存和贡献分数报告，不会在前一个已剪模型上继续剪。

## 2. 50% 权重稀疏口径

目标是 Transformer block 内七个投影矩阵：

- Attention：`q_proj/k_proj/v_proj/o_proj`
- MLP：`gate_proj/up_proj/down_proj`

这些矩阵的权重元素合计作为总预算，精确设置约 50% 为 0。Embedding、Norm、bias、LM head 不计入该 50% 口径。

默认 `paper_nonuniform`：论文贡献分数决定不同 head/FFN 通道内部应剪多少权重；默认每个单元限制在 35%–65% 稀疏率，因此没有完整单元被删除。

`uniform` 模式则每个单元都接近 50%，用于对照。

## 3. 复用 v7 response_cache

可以直接复用：

```bash
RESPONSE_CACHE_DIR=results/paper_v7_clean_s050/response_cache \
bash scripts/run_v8_paper_only_weight50.sh
```

v8 只读取其中与论文前端有关的信息：

- MLP/Attention 任务梯度响应；
- task event；
- base sample id；
- scenario id；
- NLL/position loss；
- head/channel 布局元数据。

旧缓存中的额外文件不会参与 v8 任何评分或 mask 决策。

如果修改以下设置，应重新生成 response cache：

- `paper_score_nsamples`
- `paper_calib_seqlen`
- `paper_response_length`
- `paper_scenario_ratios`
- `paper_event_bins`
- 模型、tokenizer、校准数据或任务梯度响应定义

只修改 LCB 参数、粒球融合、频带覆盖、权重预算上下界或均匀/非均匀分配时，可以复用 response cache。

## 4. 推荐启动

```bash
bash scripts/run_v8_paper_only_weight50.sh 2>&1 | tee v8_run.log
```

如果你的目录不同，可覆盖环境变量：

```bash
MODEL_PATH=/path/to/Llama-2-7b \
C4_PATH=/path/to/c4 \
WIKITEXT2_PATH=/path/to/wikitext2 \
RESPONSE_CACHE_DIR=/path/to/v7/response_cache \
OUTPUT_DIR=results/paper_v8_weight50 \
bash scripts/run_v8_paper_only_weight50.sh
```

## 5. 主要输出

`shared_score_report/` 中：

- `all_contribution_scores.csv`：MI/粒球/LCB 分数与频带贡献；
- `unit_evidence.npz`：四级消融所需的 scalar score + band contribution；
- `weight_budget_<method>.csv`：每个 head/FFN 通道的权重数、置零数和稀疏率；
- `weight_budget_<method>_layer_summary.csv`：每层实际权重稀疏率；
- `weight_budget_<method>_module_summary.csv`：每个投影矩阵实际稀疏率；
- `weight_budget_<method>.json`：全局精确稀疏率与最终频带覆盖；
- `weight_mask_overlap.csv`：四级消融最终权重 mask 的可计算重合度。

根目录输出 `ablation_ppl_summary.csv`，汇总四组 WikiText-2 PPL。

## 6. 方法边界

论文原方案最终优化变量是完整的注意力头/FFN 通道。当前 v8 按实验要求把这一最终执行层扩展为“结构单元贡献控制其内部权重保留预算”，因此统计前端和预算目标沿用论文，而最终执行粒度改为权重元素。这一扩展在实验与论文表述中应明确说明，不能把它写成论文原公式已经直接定义了逐权重重要性。
