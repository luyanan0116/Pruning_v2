# V8.2 运行说明（先基线，再消融）

## 1. 先跑 Dense / Wanda，统一评测口径

推荐先只跑：

```bash
MODEL_PATH=meta-llama/Llama-2-7b-hf \
MODEL_CACHE_DIR=/你的模型缓存目录 \
C4_PATH=/你的C4目录 \
WIKITEXT2_PATH=/你的wikitext-2-raw目录 \
bash scripts/run_v82_dense_wanda_baseline.sh
```

这一步固定：

- tokenizer：`use_fast=False`
- C4 calibration：128 samples
- seed：0
- Wanda calibration seqlen：4096
- WikiText eval seqlen：4096
- sparsity：q/k/v/o/gate/up/down 权重严格 50%

先检查 Dense PPL，再检查本包 `wanda` 是否与你单独复现 Wanda 的结果接近。若这一步对不上，不要继续调 MI/GB/LCB。

## 2. 再跑 V8.2 四组消融

```bash
MODEL_PATH=meta-llama/Llama-2-7b-hf \
MODEL_CACHE_DIR=/你的模型缓存目录 \
C4_PATH=/你的C4目录 \
WIKITEXT2_PATH=/你的wikitext-2-raw目录 \
OUTPUT_DIR=results/v82_main \
bash scripts/run_v8_paper_only_weight50.sh
```

会依次用全新模型副本运行：

```text
dense
wanda
paper_mi
paper_mi_gb
paper_mi_gb_lcb
paper_full
```

默认只使用 WikiText-2 validation 做调参比较，结果汇总：

```text
results/v82_main/ablation_ppl_summary.csv
```

论文贡献分数与预算报告：

```text
results/v82_main/shared_score_report/
```

## 3. V8.2 的核心逻辑

论文部分：

```text
任务梯度响应
  -> 频域分解
  -> MI
  -> 粒球多粒度局部估计
  -> 重复采样
  -> LCB
  -> Full中的频带覆盖修正
```

权重剪枝部分：

```text
论文贡献分数
  -> 45%~55% 单元稀疏率
  -> 全局投影，保证权重总稀疏率严格50%
  -> Wanda |W|*sqrt(input activation energy)
  -> 选择具体被清零权重
```

注意：最终剪枝对象仍然是**单个权重**，不会删除完整 attention head 或完整 FFN channel。

## 4. 与旧 V8 的两个关键区别

旧 V8：

```text
score / cost -> 分预算
单元内部 deterministic-uniform 清零
```

V8.2：

```text
score -> constrained projection -> 45%~55%
Wanda element metric -> 具体mask
```

因此 attention head 不再因为自身拥有的权重数比 FFN channel 大很多而被 `score/cost` 系统性压低。

## 5. 推荐调参顺序

先保持所有 MI/GB/LCB 参数不动，只调非均匀幅度：

```bash
# 最保守，最接近 Wanda
--paper_weight_min_unit_sparsity 0.475 \
--paper_weight_max_unit_sparsity 0.525

# 默认主实验
--paper_weight_min_unit_sparsity 0.45 \
--paper_weight_max_unit_sparsity 0.55
```

如果 `0.475~0.525` 已经比 Wanda validation PPL 更差，就说明当前贡献排序没有提供正增益，此时应该先分析 `paper_mi -> GB -> LCB` 哪一步引入了坏排序，而不是继续扩大非均匀范围。

其次可调：

```text
paper_budget_temperature: 1.5 / 1.0 / 0.75
lcb_lambda:               0.25 / 0.5 / 0.75
paper_coverage_alpha:     0.0 / 0.05 / 0.10
```

只用 validation 选定后，最终一次运行：

```bash
python run_paper_ablation.py ... --eval_wikitext_split both
```

再报告 test PPL。

## 6. 显存不足

128 × 4096 的 Wanda 顺序校准缓存较大。默认：

```bash
--wanda_activation_storage auto
```

如果 OOM：

```bash
--wanda_activation_storage cpu
```

会慢一些，但显著降低 GPU hidden-state buffer 占用。

## 7. 测试

```bash
pytest -q
```

当前构建版本通过 12 个测试，包括：严格50%、逐单元 quota 精确兑现、Wanda 顺序 mask、`score/cost` 偏置消除以及 2/3 场景抽样修复。
