# 青基 V8.1 逐项代码核对与 v5 修改报告

本项目以 `青基2026-段围V8.1(2).pdf` 为主规范，并用 `sample.pdf` 中更细的公式与实现描述补全工程细节。两份材料均属于研究方案，不是完整、唯一的算法规格，因此对于未给定的探针选择、频带权重、粒球二分算法、风险系数、覆盖系数、恢复训练等部分，本实现给出了显式、可复现实例，并全部写入 `score_config.json`。

## 0. 两种剪枝口径必须区分

- `--paper_mask_style structured_unit`：真正移除完整注意力头/FFN 中间通道，最贴近青基“结构单元削减”的语义。
- `--paper_mask_style wanda_weight`：在 q/k/v/o、gate/up/down 的权重矩阵内进行 50% Wanda 式权重置零，由论文贡献谱轻量引导。它更接近 PPL 6.5 的目标，但不是“删除 50% 完整结构单元”。

青基 V8.1 的预期研究结果写的是“结构单元削减 10%-20%”，而不是 50%。因此“严格结构单元剪除 50%”与“PPL 约 6.5”不能被视为文件本身已经承诺的组合。本项目同时保留两种模式，避免混淆实验口径。

## 1. 任务响应信号

### 文件要求

1. 对注意力头和 FFN 中间维度统一使用任务损失梯度。
2. 注意力头：对头输出梯度在 head_dim 上取 L2 范数。
3. FFN 通道：取任务损失对中间激活的梯度绝对值。
4. 沿序列位置保留一维响应序列。
5. 对每个样本、每个结构单元执行样本内标准化。

### v5 实现

- `lib/paper_pruning/collector.py`
  - FFN：在 `mlp.down_proj` 输入上捕获 `abs(dL/dA)`。
  - Attention：在 `self_attn.o_proj` 输入上捕获梯度，重排为 `[head, head_dim]` 后取 L2。
  - 对不同上下文场景做自适应池化以对齐响应长度。
- `lib/paper_pruning/mi.py`
  - 在 DCT 前按 `[样本, 单元]` 对序列位置做均值/标准差标准化。

### 修复点

v4 使用了“激活 × 梯度”，不等同于 V8.1 的梯度响应定义。v5 已改为梯度本身。

## 2. DCT 与频带能量

### 文件要求

1. 对标准化响应沿序列位置执行一维 DCT。
2. 频域能量为 DCT 系数平方。
3. 切分为 Q 个细粒度桶并对桶内能量求和。
4. 低频解释长程依赖，高频解释局部模式。

### v5 实现

- `standardize_responses()`
- `dct_frequency_energy()`
- 采用正交 DCT-II。
- 能量使用 `abs(DCT)^2`，不是幅值或均值。
- 将完整频率索引连续划入 `fine_bins` 个桶并求和。

## 3. 任务事件变量

### 文件要求

`sample.pdf` 将位置级下一个词 NLL 定义为统一任务事件刻度，并按全体分位数离散为 L 个等级。

### v5 实现

- 从模型 logits 显式计算 position-level next-token NLL。
- 对不同上下文长度池化到统一位置长度，保存为 `position_losses.npy`。
- 以每个样本-场景观测的 position-derived NLL 均值构造全局分位事件等级。
- 事件划分在所有样本和场景上统一完成，不再按每个场景单独离散。

### 说明

研究方案同时写了“位置级事件”与“样本级频带能量”，二者的配对粒度没有完全展开。v5 采用“位置 NLL 导出的样本-场景事件”完成可执行对齐，并保存完整位置 NLL，便于后续改成 token-level MI。

## 4. 任务相关性驱动的相邻频桶合并

### 文件要求

1. 每层选代表性结构单元作为 probe。
2. 计算细桶与任务事件的 MI 曲线。
3. 每轮重新计算相邻桶合并后的 MI。
4. 选择合并损失最小的相邻桶对，直到 B 个频带。

### v5 实现

- `choose_probe_units()` 按层内响应活跃度的分位位置抽取代表性单元，覆盖低、中、高活跃度。
- `adaptive_merge_adjacent_bins()` 每轮真正计算：
  `loss = r(left) + r(right) - r(union)`。
- 合并后重新计算 union MI，不再用相邻标量差值近似。
- 频带边界始终连续且按低频到高频排列。

## 5. MI 主估计器与 KDE 辅估计器

### 文件要求

- kNN 是主估计器，用于贡献谱和剪枝排序。
- KDE 是辅助估计器，用于一致性检查与备选。
- 连续响应 + 离散事件采用熵分解形式。
- 粒球内 KDE 带宽与粒球半径成比例。

### v5 实现

- `estimate_knn_mi_matrix()`：主排序输出。
- `estimate_kde_mi_matrix()`：辅助诊断，不再与 kNN 简单平均。
- 所有 CSV 同时导出 kNN 和 KDE 频带结果。
- 局部 KDE 的 bandwidth 由粒球半径与维度共同确定。

## 6. 粒球构建与多粒度局部化

### 文件要求

1. 以每个结构单元的 B 维频带响应向量构建响应空间。
2. 粒球中心为球内均值，半径为样本到中心最大 L2 距离。
3. 用几何紧致度与事件纯度控制划分。
4. 当样本数满足下限且纯度不足时继续分裂。
5. 控制分裂深度形成嵌套多粒度集合。
6. 每个粒球内部单独估计 MI。

### v5 实现

- 默认 `--paper_gb_localization unit_local`，每一层、每一种结构类型、每一个结构单元都独立构球。
- 二均值自适应分裂，强制最小球样本数、最大深度、最大球数。
- 纯度阈值 `(0.65, 0.75, 0.85)` 形成嵌套粗-中-细粒度快照。
- 默认 `min_event_classes=2`，避免纯球导致局部 MI 数学退化为 0。
- `layer_shared` 仅保留为快速调试近似，不是严格模式。

## 7. 局部 MI、球权重和多粒度融合

### 文件要求

1. 球内估计频带响应与事件变量的 MI。
2. 同一粒度按球样本占比加权。
3. 不同粒度融合权重由估计方差或排序稳定性自适应决定。

### v5 实现

- 每球局部 MI 乘 `|S_g| / N` 后加权汇总。
- 粒度权重默认使用 inverse-dispersion，自适应偏向低方差粒度。
- 同时输出每个粒度的 `fusion_weight` 和 `dispersion`。
- 不再固定等权融合。

## 8. 样本-场景双源重复估计与 LCB

### 文件要求

1. 样本侧抽样误差与场景侧输入变化共同进入重复估计。
2. 每次重复重新计算贡献分数。
3. 由经验分布计算均值、样本标准差。
4. `LCB = mean - lambda * std`。

### v5 实现

- `dual_source_bootstrap_indices()`：
  - 第一阶段按 base calibration sample 有放回抽样；
  - 第二阶段在每个选中样本内抽样场景；
  - 不再把同一原始样本衍生的多个场景错误视为独立样本。
- 每次重复重新执行：频带合并、粒球划分、局部 MI、多粒度融合。
- 标准差使用 `ddof=1`。
- 同时导出总分 LCB 和逐频带 LCB 均值/标准差。

## 9. 频带覆盖与预算约束的贪心配置

### 文件要求

1. 在预算内最大化 LCB 总贡献。
2. 各频带满足最低覆盖比例。
3. 欠覆盖频带获得补偿权重。
4. 使用边际增益贪心选择保留单元。

### v5 实现

- `lib/paper_pruning/budget.py`
- 先为每个非空频带播种至少一个覆盖单元，避免整个频段被饿死。
- 然后按“贡献排序项 + 欠覆盖补偿项 / cost”进行批量贪心。
- 返回实际频带覆盖率并写入控制台。
- `paper_mi_gb_lcb` 使用逐频带 `mean - lambda*std` 进入覆盖约束。

## 10. 低 PPL 路径的关键修复

v4 高 PPL 的主要工程原因不是 MI/GB/LCB 公式本身，而是 mask 生成破坏了 Wanda 的低损失工作点：

1. v4 一次性使用剪枝前激活统计生成所有层 mask。
2. v4 贡献乘子范围过大，可能覆盖 Wanda 的权重-激活指标。
3. v4 各结构单元行稀疏率可在很宽范围内波动。
4. 评估长度未严格锚定 Wanda 常用的 2048。

v5 修复：

- `wanda_sequential.py` 按官方 Wanda 的层序执行：前层剪完后重新传播校准输出，再统计下一层激活。
- 默认贡献乘子范围约 `[0.990, 1.010]`。
- 默认单元行预算仅在 50% 周围约 `49.5%-50.5%` 微调。
- 设 `--paper_wanda_row_spread 0 --paper_wanda_guidance_strength 0` 时，回退为严格 Wanda row-wise mask，用于诊断 PPL 基线。
- 默认评估与 Wanda 校准长度均为 2048。

## 11. 输出文件

- `all_contribution_scores.csv`：所有结构单元的 MI、GB、LCB、kNN/KDE 逐频带分数与剪枝标志。
- `granular_ball_summary.csv`：各粒度球数、纯度、半径、深度、融合权重、离散度。
- `prune_indices.json`：三种消融的结构单元索引。
- `prune_batches_step10.json`：每层每 10 个索引一批。
- `mask_overlap.csv`：三种方法的结构选择重合度。
- `weight_mask_summary_*.csv`：七个线性矩阵的真实权重稀疏率。
- `*_local_balls.png`：严格 unit-local 模式下低贡献、剪枝边界、高贡献代表单元的实际局部粒球图。

## 12. 不能诚实保证的事项

- 本代码包所在环境没有用户服务器上的 Llama-2-7B、C4、WikiText-2 与 A100，因此未执行完整 7B PPL 实验。
- 不能预先保证 `paper_mi_gb_lcb` 必然为 6.5。达到该数值首先要求同一环境中的原始 Wanda 50% 基线本身接近 6.5。
- 若 `wanda` 基线明显高于 6.5，应先检查模型版本、WikiText 文本、tokenizer、评估长度和数据预处理，而不是继续调 MI/GB/LCB。
- 严格删除 50% 完整注意力头与 FFN 通道通常不会接近 6.5；目标 6.5 应使用 `wanda_weight` 口径。
