# 代码与两份研究方案逐公式对齐审计

## 审计结论

当前版本已按两份材料的并集流程重构为：

`任务损失梯度响应 → 样本内标准化 → 原生长度 DCT-II → 平方频率能量 → 细桶 → 任务相关相邻合并 → 频带互信息贡献谱 → 逐结构单元粒球多粒度局部估计 → 样本—场景双源重复估计 → 方差自适应多粒度融合 → 均值/标准差/LCB → 频带覆盖与多维部署预算约束 → 贪心配置生成 → 完整注意力头/前馈通道裁剪 → 剪前剪后与临界区间验证`。

代码中不包含权重级非结构化评分、权重级掩码、按单个权重重新分配稀疏率或相关回退路径。

## 原版本不一致或缺失的环节及本次补充

| 环节 | 原版本问题 | 当前处理 |
|---|---|---|
| 频域输入长度 | 在 DCT 前把不同上下文响应压到固定长度，会改变材料中的位置频率含义 | 每条样本—场景按原生长度直接标准化并执行 DCT-II，只在频率轴上汇聚为固定数量细桶 |
| DCT 与能量 | 生产路径与公式路径分散，缓存不便核验 | 生产缓存直接保存公式 (4)-(7) 后的细桶平方能量，并保留原生序列长度 |
| 任务相关频带 | 仅固定切带或未完整实现探针驱动合并 | 增加探针聚合、细桶互信息、相邻合并损失与贪心合并 |
| 互信息主估计器 | 使用通用依赖代理或与材料熵分解不一致 | 实现 `H(Z)-Σp(c)H(Z|c)` 与 Kozachenko–Leonenko 近邻熵；KDE 作为辅助 |
| 粒球响应空间 | 对频带向量额外做跨样本标准化，与材料的 `v_u(x)` 不一致 | 改为直接在每个结构单元自己的原始 B 维频带响应空间中构球 |
| 粒球粒度融合 | 用球间离散度代替材料所述“估计方差或排序稳定性” | 每次重复保留各粒度估计，以重复估计经验方差计算逆方差融合权重 `ρ_m` |
| 双源不确定性 | 场景重采样可能从不存在的基础样本—场景组合回退到其他场景 | 两阶段配对抽样只从每个基础样本真实存在的场景实现中抽取 |
| LCB | 仅对最终总分做近似修正或固定一次粒球划分 | 每次重复重新做频带合并、粒球划分与局部 MI，再计算均值、样本标准差和 LCB |
| 全局配置 | 仅局部/逐层比例或单一资源约束 | 将所有头和前馈通道放入全局候选表，同时强制参数、FLOPs、显存、KV-cache、实测时延预算与频带覆盖 |
| 边际增益 | 未实现欠覆盖动态补偿 | 按材料公式动态更新 `w_b(K)`，逐单元选择最大边际增益 |
| 结构执行 | 只改变评分或生成权重掩码，未完整删除耦合结构 | 前馈通道同步处理 gate/up 行与 down 列；注意力头同步处理 q/k/v 行与 o 列 |
| 闭环验证 | 只报告剪后 PPL | 增加剪前/剪后贡献谱、排序一致性、多场景 NLL/PPL、多上下文、多种子、最大稳定剪枝率与性能突降区间 |
| 缓存复验 | 仅按模型形状判断，可能误复用同结构的旧缓存 | 缓存签名加入模型、分词器、校准数据和场景清单的本地文件大小/修改时间指纹 |

## 公式覆盖范围

- 详细材料的梯度响应、样本内标准化、DCT、平方能量、细桶、位置 NLL、分位事件、探针聚合、相邻合并损失、频带 MI、总贡献、粒球中心/半径/纯度、局部 MI、样本占比汇聚、多粒度融合、重复估计、均值/标准差、LCB、k 近邻熵分解、KDE、覆盖约束、预算约束和边际增益均有对应实现。
- 双源材料补充的样本侧与场景侧不确定性、配对重复估计、部署预算配置和最大剪枝率适用边界均有对应实现。
- 精确代码位置见 `FORMULA_TRACEABILITY.md`。

## 唯一需要公开的材料口径缺口

详细材料把任务事件写成位置级 `Y'_t`，但频带能量是样本—场景级 `Z_u^(b)(x)`，没有给出从位置级事件到样本级频带向量的唯一配对公式。当前实现：

1. 完整保存每个位置的 next-token NLL 与位置级分位事件；
2. 用每条样本—场景的平均位置 NLL做全局分位离散，得到互信息估计使用的样本级事件；
3. 在缓存元数据中固定记录该定义。

这样避免把同一频带向量复制到每个 token 后造成伪重复样本。除这一材料本身未唯一规定的对齐点外，主流程与可执行公式已逐项落实。

## 静态验证

- Python 全量编译检查通过。
- 单元测试覆盖 DCT 余弦公式、平方能量、k 近邻熵分解、任务相关频带合并、原始粒球响应空间、方差自适应融合、双源抽样、LCB、全局多预算贪心、完整结构单元处理和场景清单。
- 发布前检查源代码与文档中不存在被移除的权重级基线组件名称。

## 尚未在本环境完成的实验验证

本环境没有用户实际使用的模型权重、完整 C4/WikiText 数据和目标 GPU，因此没有声称已经复现某个具体 PPL、吞吐或时延数值。代码层面对齐不等于实验数值已复现；最终结果仍需在同一模型、分词器、校准样本、随机种子、上下文长度和硬件环境下运行。

## v9 三阶段消融隔离修正

旧版虽然输出 `paper_mi`、`paper_mi_gb`、`paper_mi_gb_lcb` 三组分数，但统一评分入口总是执行粒球和重复估计，而且 `paper_mi_gb` 的多粒度融合权重默认来自重复估计方差。因此其 PPL 可以用于方法结果比较，但不能严格解释为“只增加粒球、再只增加 LCB”的逐组件消融。

v9 改为按请求方法执行：

- `paper_mi`：只执行互信息贡献谱；
- `paper_mi_gb`：执行互信息、粒球与多粒度融合；在 `equal`/`manual` 模式下不调用重复估计；
- `paper_mi_gb_lcb`：在前一阶段基础上增加双源重复估计和 LCB。

`run_paper_ablation.py` 默认使用 `equal` 粒度权重并检查逐层执行清单，确保第二组未混入重复估计、第三组才引入 LCB。研究方案中的方差自适应融合仍通过 `repeat_variance` 模式保留，但不作为严格三阶段消融默认值。
